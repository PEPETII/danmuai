- 根据两张用户报错截图与基元律动平台调用记录，完成根因诊断。
- 定位到 `app/openai_chat_stream.py` 流式 HTTP 错误处理未先 `read()` 就读取响应体，导致 `RuntimeError: Attempted to access streaming response content...`。
- 同时指出 OpenAI Chat Completions 路径缺少 Content-Type 检查与 `stream_options` 兼容处理，可能是基元律动 200 但返回为空的原因。
- 产出诊断报告：`reports/DanmuAI_Issue_Diagnosis_Streaming_Response_2026-09-05.md`（未做代码修改）。

- 第二轮诊断：`python main.py` 启动后界面不显示 + 托盘右键菜单打不开。
- 架构确认：桌面界面是 pywebview 子进程承载的窗口（`main_lifecycle_mixin.py:499` 调度），托盘是 Qt 主线程 `QSystemTrayIcon`（`main_lifecycle_mixin.py:385` show）。
- 托盘右键打不开：菜单仅用 `setContextMenu` 绑定，而 `_on_activated`（`tray.py:418-423`）不处理 `Context` 原因，Windows 11 / 溢出区下不可靠，且无 `popup` 兜底。
- 界面不显示：主因单实例守护命中后台旧实例（`ACTIVATED_EXISTING` 静默 exit）与 WebView2 缺失导致 pywebview 回退浏览器/defer 跳过；辅以子进程崩溃或握手超时。
- 产出诊断报告：`reports/DanmuAI_Issue_Diagnosis_Tray_NoWindow_2026-09-05.md`（未做代码修改）。
