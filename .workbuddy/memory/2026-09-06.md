# 2026-09-06

## 烂梗公式化页 · 新用户友好布局原型
- 任务：侧边栏【公式化弹幕】→ tab【烂梗公式化】对新用户不友好，需生成 HTML 展示合理分布。
- 先读真实代码梳理字段（不参考任何现有 HTML 视觉）：
  - `web/static/modules/app-meme-barrage-page.js`：表单逻辑
  - `app/meme_barrage/config.py`：取值枚举与范围（category: random/tagged/local；display_mode: full/ai；采集/展示 间隔 1–60s、批量 1–100/1–50；MAX_TAGS=3）
  - `app/meme_barrage/client.py`：27 个真实标签（FALLBACK_TAGS，默认 06 群魔乱舞）
  - `web/static/partials/content-pages.html`：当前面板字段语义
- 产出：`reports/meme_barrage_newuser_layout.html`（从零设计的交互式原型）。
- 新布局核心改进：①顶部"这是什么/三步流程"说明卡；②总开关关闭时其余变灰禁用；③分类模式改 radio 为带说明+推荐徽章的卡片，标签选择改为带"示例句"的 chip 并显式计数 1/3；④显示形式两卡片并标注资源成本；⑤采集/展示节奏改滑块+实时"预计上屏频率"预估+屏幕滚动预览+预设（慢/均衡/快）；⑥本地库状态卡 + 清除二次确认；⑦底部 sticky 保存/恢复默认 + 新手指引气泡。
- 约束：遵守 MEMORY.md 中"禁止复用项目已有 HTML 作为视觉参考"，本原型为从零原创样式。

## 烂梗公式化页 · 正式落地（项目风格）
- 用户修改了原型后允许正式改项目，约束：用项目自身风格（Tailwind Play CDN + warm-tokens-* 语义 token），禁用原型自定义 CSS。
- 用户在原型中删去了「顶部说明卡」「屏幕预览」，保留并改项目风格：总开关联级禁用、分类/显示形式卡片、带示例标签 chip、节奏滑块+预设+频率预估；并确认：保留本地库段落、新增【恢复默认】按钮。
- 改动文件：
  - `web/static/partials/content-pages.html`：重写【烂梗公式化】面板（#memeSettingsArea 联级禁用；.meme-opt-card 卡片；#memeTagGrid/.meme-tag-chip 带示例+计数；#memeFreqNum 频率预估；保留本地库+清除；#btnResetMemeBarrageSettings 恢复默认）。
  - `web/static/modules/app-meme-barrage-page.js`：重写逻辑对接新结构（lock、卡片选中态 syncCardSelected、renderMemeTagGrid、updateMemeFreq、预设、resetMemeBarrageSettings、clear 加 confirm）。
  - `web/static/warm-tokens-danmu-pool.css`：追加新组件样式（全用 --color-* token，暗色自适应）。
  - 重跑 `web/static/build_index_html.py` 重新生成 `web/static/index.html`（运行服务读 index.html，非 template）。
- 校验：node --check JS 通过；index.html 含新 ID、无旧折叠 ID（memeDisplayModeAccordionTrigger 等）。未动 dist/ 打包产物（发布构建另走流程）。
- 注意：CSS 经 warm-tokens.css @import 引入，无需重建；标签示例句为说明用途（非真实弹幕内容）。
