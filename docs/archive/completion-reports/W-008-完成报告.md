# Codex 完成报告

> 工单 ID：W-008  
> 完成时间：2026-05-29  
> 执行者：Cursor Agent

---

## 1. 修改摘要

在「温馨控制台」（`#page-overview`）顶部增加可关闭的公告简略条：展示 Supabase 列表首条公告的「标题：正文」前 30 字；关闭时写入 `localStorage` 键 `danmu_announcements_overview_banner_dismissed_id`，同一 `id` 不再显示直至新公告成为首条。与侧栏未读红点（`read_ids` / `last_seen_ms`）独立。`refreshAnnouncementsUnreadBadge` 单次拉取同时更新红点与顶栏。

## 2. 修改的文件

- `web/static/index.html`
- `web/static/app.js`
- `web/static/warm-tokens.css`
- `tests/test_bundle_paths.py`
- `docs/WEB_CONSOLE.md`
- `docs/工单列表.md`
- `docs/当前仓库状态.md`
- `docs/templates/工单/W-008-控制台顶部公告简略条.md`
- `docs/templates/Codex完成报告/W-008-完成报告.md`

## 3. 未修改的关键区域

- 未修改 `app/`、`main.py`：是
- 未修改 `supabase/` 迁移、`requirements.txt`：是
- 未修改公告已读 API（`announcements-read-state`）语义：是

## 4. 运行的命令

```bash
python -m pytest tests/test_bundle_paths.py tests/test_supabase_static.py -q
```

## 5. 构建/测试结果

| 检查项 | 结果 | 说明 |
|--------|------|------|
| pytest（bundle + supabase static） | 通过 | 见命令输出 |
| boundary_guard | 未运行 | 未改 `app/` 编排 |
| 真实 Supabase | 未运行 | 需本机 `supabase-config.js` |

## 6. 手动验证步骤

| 步骤 | 预期 | 实际 | 通过 |
|------|------|------|------|
| 有公告且未关闭 | 控制台顶栏显示截断文案 | 待负责人 | — |
| 点叉后 F5 | 同一 id 不显示 | 待负责人 | — |
| 发布新公告 | 顶栏再现 | 待负责人 | — |
| 未配置 Supabase | 顶栏不出现 | 待负责人 | — |
| 侧栏红点 | 与改前一致 | 待负责人 | — |

## 7. 风险与注意事项

- 仅修改同一条公告正文（`id` 不变）时，已关闭用户不会再次看到顶栏。
- 拉取失败时顶栏静默隐藏；侧栏红点保持 catch 前状态（与 ISSUE-007 一致）。

## 8. 发现但未处理的问题

无新范围外问题。ISSUE-007（公告 REST 失败 Console 噪音）仍为待办。

## 9. 已更新的文档

- [x] `docs/WEB_CONSOLE.md`
- [x] `docs/当前仓库状态.md`
- [x] `docs/工单列表.md`

## 10. 建议下一个工单

可选：ISSUE-007 公告加载全局静默降级。
