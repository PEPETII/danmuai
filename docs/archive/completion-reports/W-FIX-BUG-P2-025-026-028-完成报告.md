# Codex 完成报告

> 工单 ID：W-FIX-BUG-P2-025-026-028  
> 完成时间：2026-06-03  
> 执行者：Codex

---

## 1. 修改摘要

修复 P2 BUG-025/026/028：**BUG-025** 采用方向 A——`show_for_screen` 在有效屏列表内钳制无效 `screen_index` 并同步 geometry，`DanmuApp.start()` 在显示后改调 `ensure_render_loop()`（保留隐藏窗口上直接 `start_render_loop` 不误启 timer）；**BUG-026** 删除未引用的 `_calc_auto_interval`；**BUG-028** 移除 `DanmuAppStateMixin` 上三处 timing/scheduler 兼容 `@property`，测试与 `main._maybe_inject_local_fallback` 改走 `get_request_scheduler()` / `get_request_timing_service()`。

## 2. 修改的文件

- `app/overlay.py`
- `main.py`
- `app/main_state_mixin.py`
- `tests/test_overlay_render.py`
- `tests/conftest.py`
- `tests/test_request_scheduling.py`
- `tests/test_api_schedule.py`
- `tests/test_diagnostics_snapshot.py`
- `tests/test_danmu_dedup.py`（范围外最小修补：`start()` 改调 `ensure_render_loop` 后 overlay 桩需同名方法）
- `docs/bug-audit/BUGS-OVERVIEW.md`
- `docs/bug-audit/TEST-GAPS.md`
- `docs/当前仓库状态.md`
- `docs/工单列表.md`
- `docs/templates/Codex完成报告/W-FIX-BUG-P2-025-026-028-完成报告.md`

## 3. 未修改的关键区域

- 未修改 `web/static/**`：是
- 未修改 `community-site/**`：是
- 未修改 `app/web_api/*`、`app/web_console.py`：是
- 未修改 `scripts/boundary_guard` 语义：是
- 未删除 `_request_scheduler` / `_request_timing_service` 服务本体：是
- 未修复 BUG-027、BUG-029 等其它 P2：是

## 4. 运行的命令

```bash
ruff check app main.py tests scripts
python -m pytest tests/test_overlay_render.py tests/test_request_scheduling.py tests/test_api_schedule.py tests/test_diagnostics_snapshot.py -q
python -m pytest tests/ -q
python scripts/boundary_guard.py
rg "_calc_auto_interval" main.py
rg "_last_api_trigger_at|_request_started_at_by_id|_rtt_history" main.py app/main_state_mixin.py
```

## 5. 构建/测试结果

| 检查项 | 结果 | 说明 |
|--------|------|------|
| ruff（全仓库） | 2 处既有问题 | `app/region_selector.py` F401、`scripts/rebalance_t008_tests2.py` I001；非本工单改动 |
| 工单 scoped pytest | 通过 | **48 passed**（overlay 19 + scheduling/api/diagnostics） |
| pytest 全量 | 4 failed | **873 passed, 5 skipped**；失败均为 `tests/test_pool_topup.py` 引用不存在的 `main.sample_danmu_for_config`（范围外既有问题） |
| boundary_guard | 通过 | `Boundary Guard: PASS` |
| `rg _calc_auto_interval main.py` | 无匹配 | |
| `rg` façade main/mixin | 无匹配 | |

## 6. 手动验证步骤

| 步骤 | 预期 | 实际 | 通过 |
|------|------|------|------|
| `pytest tests/test_overlay_render.py …` | 全绿 | 48 scoped 通过 | 是 |
| `boundary_guard.py` | PASS | PASS | 是 |
| 无效 `screen_index` 启动弹幕动画 | 钳制到有效屏后可刷新 | 由单测 `test_show_for_screen_invalid_index_starts_render_with_content` 覆盖 | 是（自动化） |
| `rg` façade 已从 main/mixin 移除 | 无定义 | 无匹配 | 是 |

## 7. 风险与注意事项

- `docs/runtime-state-map.md` 仍描述 DanmuApp 兼容 façade，本工单未授权修改该文档，需后续文档复核工单对齐。
- 全量 pytest 中 `test_pool_topup.py` 失败与本次改动无关，建议单独开工单修复 `sample_danmu_for_config` 导入路径。

## 8. 发现但未处理的问题

| 问题 ID | 简述 | 已记录 |
|---------|------|--------|
| （待负责人分配） | `tests/test_pool_topup.py` 四处 monkeypatch `main.sample_danmu_for_config` 不存在 | 否（本报告引用；可记入已知问题） |
| runtime-state-map 文档滞后 | façade 移除后文档仍写兼容委托 | 否（完成报告 §7） |

## 9. 已更新的文档

- [x] [docs/bug-audit/BUGS-OVERVIEW.md](../../bug-audit/BUGS-OVERVIEW.md)
- [x] [docs/bug-audit/TEST-GAPS.md](../../bug-audit/TEST-GAPS.md)
- [x] [docs/当前仓库状态.md](../../当前仓库状态.md)
- [x] [docs/工单列表.md](../../工单列表.md)
- [x] 本完成报告

## 10. 建议下一个工单

- 修复 `tests/test_pool_topup.py` 与 `main.sample_danmu_for_config`（或恢复/迁移该 helper 至 `app/danmu_pool`）。
- 文档工单：同步 `docs/runtime-state-map.md` 中 BUG-028 相关 façade 描述。
