# Codex 完成报告

> 工单 ID：W-021  
> 完成时间：2026-05-29  
> 执行者：Cursor Agent

---

## 1. 修改摘要

修复 `DefaultOpenAIAdapter.patch_openai_chat_body`：仅在请求体 `stream: true` 时写入 `stream_options.include_usage`。非流式 `api_probe`（`stream: false`）不再误带该字段，百炼/DashScope compatible-mode「测试连接」不再稳定触发上游 HTTP 400。主链路 `_request_openai` 仍为流式 + `stream_options`，DashScope usage 统计行为不变。

## 2. 修改的文件

- `app/providers/adapters/default_openai.py`
- `tests/test_provider_adapters.py`
- `tests/test_api_probe.py`
- `docs/templates/工单/W-021-api_probe非流式stream_options.md`
- `docs/templates/Codex完成报告/W-021-完成报告.md`
- `docs/templates/已知问题记录/ISSUE-015-api_probe非流式stream_options.md`
- `docs/工单列表.md`
- `docs/当前仓库状态.md`
- `docs/已知问题与后续事项.md`
- `docs/architecture/provider-adapter.md`

## 3. 未修改的关键区域

- 未修改 `main.py`：是
- 未修改 `web/`：是
- 未修改 `app/ai_client.py` 主链路 HTTP/SSE：是
- 未修改 `app/api_probe.py` 调用方式（仍非流式 ping）：是
- 未修改 `app/providers/capabilities.py`：是

## 4. 运行的命令

```bash
python -m pytest tests/test_provider_adapters.py tests/test_api_probe.py tests/test_ai_client.py -q
python -m pytest tests/ -q
```

## 5. 构建/测试结果

| 检查项 | 结果 | 说明 |
|--------|------|------|
| pytest（相关套件） | 通过 | `49 passed`（`test_provider_adapters` + `test_api_probe` + `test_ai_client`） |
| pytest（全量） | 部分失败 | `677 passed, 8 failed`；失败用例与 Qt `LogEmitBus`/弹幕池 API 相关，非本工单改动 |
| boundary_guard | 未运行 | 本工单未触达 main/编排 |

## 6. 手动验证步骤

| 步骤 | 预期 | 实际 | 通过 |
|------|------|------|------|
| 百炼预设 + 有效 Key → 测试连接 | `ok: true` | 待负责人真实 Key 复验 | 待负责人 |
| 开始弹幕后 AI 弹幕 | 仍正常 | 待负责人复验 | 待负责人 |

## 7. 风险与注意事项

- 流式主链路仍带 `stream_options`；`test_request_openai_*` 回归覆盖。
- `openai_compatible_request_extensions` 在无 `stream` 键时不再误加 `stream_options`（更符合 OpenAI 语义）。

## 8. 发现但未处理的问题

| 问题 ID | 简述 | 已记录 |
|---------|------|--------|
| ISSUE-005 | MiMo 连通性探测不含识图 | 是（既有，非本工单目标） |

## 9. 已更新的文档

- [x] [docs/当前仓库状态.md](../../当前仓库状态.md)
- [x] [docs/工单列表.md](../../工单列表.md)
- [x] [docs/已知问题与后续事项.md](../../已知问题与后续事项.md)
- [x] [docs/architecture/provider-adapter.md](../../architecture/provider-adapter.md)

## 10. 建议下一个工单

- W-020：ISSUE-014 场景探测恢复
- 可选：ISSUE-005 MiMo 最小 vision probe
