# Codex 完成报告

> 工单 ID：T009-split-diagnostic-related-code  
> 完成时间：2026-06-03  
> 执行者：Cursor Agent

---

## 1. 修改摘要

将 1337 行的 [`scripts/boundary_guard.py`](../../../scripts/boundary_guard.py) 拆分为 `scripts/boundary_guard/` 包：常量、git diff 工具、源码解析辅助、按规则域划分的 `rules/*.py`、`runner` / `reporters` / `cli`，原脚本保留为薄兼容入口（`python scripts/boundary_guard.py` 与 `from scripts.boundary_guard import run_boundary_guard` 不变）。评估后 [`app/web_console.py`](../../../app/web_console.py) 诊断逻辑已委托 `DiagnosticsHub` 与 `web_api/routes`，无需本工单再拆。

## 2. 修改的文件

- `scripts/boundary_guard.py`（改为 CLI shim）
- `scripts/boundary_guard/__init__.py`（新建）
- `scripts/boundary_guard/models.py`（新建）
- `scripts/boundary_guard/constants.py`（新建）
- `scripts/boundary_guard/git_diff.py`（新建）
- `scripts/boundary_guard/source_parse.py`（新建）
- `scripts/boundary_guard/runner.py`（新建）
- `scripts/boundary_guard/reporters.py`（新建）
- `scripts/boundary_guard/cli.py`（新建）
- `scripts/boundary_guard/rules/__init__.py`（新建）
- `scripts/boundary_guard/rules/web.py`（新建）
- `scripts/boundary_guard/rules/config.py`（新建）
- `scripts/boundary_guard/rules/runtime.py`（新建）
- `scripts/boundary_guard/rules/request.py`（新建）
- `scripts/boundary_guard/rules/pipeline.py`（新建）
- `scripts/boundary_guard/rules/diagnostics.py`（新建）
- `scripts/boundary_guard/rules/status.py`（新建）
- `scripts/boundary_guard/rules/baseline.py`（新建）

## 3. 未修改的关键区域

- 未修改 `app/`：**是**
- 未修改 `web/`：**是**
- 未修改 `main.py`：**是**
- 未修改 `app/danmu_engine.py`：**是**
- 未修改 `app/overlay.py`：**是**
- 未修改 `app/ai_client.py`：**是**
- 未修改 `app/web_console.py`：**是**（诊断已足够薄，见 §7）

## 4. 运行的命令

```bash
python scripts/boundary_guard.py
python -m pytest tests/test_boundary_guard.py -q
python -m pytest tests/test_acceptance_gates.py::test_boundary_guard_passes_on_repository_root -q
python -m pytest tests/ -q
ruff check scripts/boundary_guard scripts/boundary_guard.py
```

## 5. 构建/测试结果

| 检查项 | 结果 | 说明 |
|--------|------|------|
| boundary_guard CLI | 通过 | `Boundary Guard: PASS` |
| pytest test_boundary_guard | 通过 | 33 passed |
| pytest acceptance gate | 通过 | 含 `test_boundary_guard_passes_on_repository_root` |
| pytest 全量 | 通过 | 874 passed, 5 skipped |
| ruff（本工单范围） | 通过 | `scripts/boundary_guard*` |

注：全仓库 `ruff check app main.py tests scripts` 仍有工单范围外既有告警（如 `main.py` 未使用 import），本票未改。

## 6. 手动验证步骤

| 步骤 | 预期 | 实际 | 通过 |
|------|------|------|------|
| 1. 各拆分文件行数 &lt;500 | 是 | 最大 `constants.py` 307 行 | 是 |
| 2. `python scripts/boundary_guard.py` | PASS | PASS | 是 |
| 3. `pytest tests/` | 无失败 | 874 passed, 5 skipped | 是 |

## 7. 风险与注意事项

- **包/文件同名**：`import scripts.boundary_guard` 解析到包 `__init__.py`；直接执行 `python scripts/boundary_guard.py` 走 shim（含 `sys.path` 注入）。
- **行为**：规则逻辑为机械搬迁，check 顺序与排序与拆分前一致。
- **主链路**：无影响（仅工具脚本）。

## 8. 发现但未处理的问题

| 问题 ID | 简述 | 已记录 |
|---------|------|--------|
| — | 无范围外问题 | — |

## 9. 已更新的文档

- 本完成报告

## 10. 建议下一个工单

- 无（工单标明无需后续任务）
