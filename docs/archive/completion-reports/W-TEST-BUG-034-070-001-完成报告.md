# Codex 完成报告

> 工单 ID：W-TEST-BUG-034-070-001  
> 完成时间：2026-06-03  
> 执行者：Codex

---

## 1. 修改摘要

闭合 [TEST-GAPS.md](../../bug-audit/TEST-GAPS.md) §5 BUG-034/070：为 `_rebuild_visibility_counts` / `visible_display_count` / `deficit_below_min` 与 1000+ 条 `engine.update()` 补充回归与 `time.perf_counter` 性能上限测试。本票**仅**改测试与文档，不优化 `DanmuEngine` 实现。

## 2. 修改的文件

- `tests/test_danmu_engine.py`
- `tests/test_danmu_motion.py`
- `tests/test_pool_topup.py`
- `docs/bug-audit/TEST-GAPS.md`
- `docs/当前仓库状态.md`
- `docs/工单列表.md`
- `docs/templates/Codex完成报告/W-TEST-BUG-034-070-001-完成报告.md`

## 3. 未修改的关键区域

- 未修改 `app/danmu_engine.py`：是
- 未修改 `main.py`：是
- 未修改 `web/static/**`：是
- 未修改 `community-site/**`：是
- 未修改 `supabase/**`：是

## 4. 运行的命令

```bash
python -m pytest tests/test_danmu_engine.py tests/test_danmu_motion.py tests/test_pool_topup.py -q
```

## 5. 构建/测试结果

| 检查项 | 结果 | 说明 |
|--------|------|------|
| scoped pytest（三文件） | 通过 | **55 passed** |
| boundary_guard | 未跑 | 纯 `tests/` + 文档，未触达 Web/API 边界 |

## 6. 手动验证步骤

| 步骤 | 预期 | 实际 | 通过 |
|------|------|------|------|
| 1 | 跑 scoped pytest 三文件 | 全部通过 | **55 passed** | 是 |
| 2 | 新增 5 条用例均可单独收集 | 无 import/Qt 阻塞 | 5/5 passed（见 §5 前单测跑） | 是 |
| 3 | scoped diff 仅允许路径 | 无 `app/` / `main.py` | 见 §9 | 是 |

## 7. 风险与注意事项

- 耗时阈值为**防回归雪崩**（`UPDATE_BUDGET_SEC=3.0`、`DEFICIT_BUDGET_SEC=2.0`），非生产 SLA。
- 审计 BUG-070 写「每帧 O(n) 全表扫描」；源码中 `Track.update()` 为 per-item `_refresh_item_visibility`，全量重建主要在 `needs_refill()`（本票 `test_needs_refill_always_rebuilds_visibility` 已记录）。

## 8. 发现但未处理的问题

| 问题 | 说明 | 已记录 |
|------|------|--------|
| BUG-034/070 产品性能优化 | `needs_refill()` 无条件 `_rebuild_visibility_counts`；真正 O(1)/降频需另票授权 `app/danmu_engine.py` | 否 |
| `BUGS-OVERVIEW.md` 仍标 BUG-034/070 待修复 | 本票只闭合测试缺口 | 否 |
| TEST-GAPS §5 `dirty_rect_union` / 透明度分桶 | 仍缺 overlay 单测 | 否 |

## 9. scoped diff 结论

本票改动路径（实现时核对）：

- `tests/test_danmu_engine.py`
- `tests/test_danmu_motion.py`
- `tests/test_pool_topup.py`
- `docs/bug-audit/TEST-GAPS.md`
- `docs/当前仓库状态.md`
- `docs/工单列表.md`
- `docs/templates/Codex完成报告/W-TEST-BUG-034-070-001-完成报告.md`

未触及 `app/**`、`main.py`、`web/static/**`、`community-site/**`、`supabase/**`。

## 10. 已更新的文档

- [x] [docs/bug-audit/TEST-GAPS.md](../../bug-audit/TEST-GAPS.md)
- [x] [docs/当前仓库状态.md](../../当前仓库状态.md)
- [x] [docs/工单列表.md](../../工单列表.md)
- [x] 本完成报告

## 11. 建议下一个工单

- 渲染层：`dirty_rect_union` / 透明度分桶 overlay 单测（TEST-GAPS §5 剩余项）。
- 性能实现票：优化 `needs_refill` / pool topup 可见性计数热路径（需授权 `app/danmu_engine.py`）。
