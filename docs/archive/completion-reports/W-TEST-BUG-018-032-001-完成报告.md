# Codex 完成报告

> 工单 ID：W-TEST-BUG-018-032-001  
> 完成时间：2026-06-03  
> 执行者：Codex

---

## 1. 修改摘要

闭合 [TEST-GAPS.md](../../bug-audit/TEST-GAPS.md) §6 BUG-018/032：在 CI 可跑范围内补充 mic poll 与 ring buffer 并发压测（500× `try_snapshot_pcm_ms(600)`，单次 &lt;50 ms）、`sounddevice status` 写入 `last_error` 的可观测回归，以及 `_sync_mic_service` 在弹幕暂停且采集已开时不调用 `sync(enabled=False)` 的契约测试。BUG-018 节奏修复已由 `W-REFACTOR-BUG-P0P1-014` 完成；本票**仅**测试与文档。

## 2. 修改的文件

- `tests/test_mic_utterance.py`
- `tests/test_mic_mode.py`
- `docs/bug-audit/TEST-GAPS.md`
- `docs/当前仓库状态.md`
- `docs/工单列表.md`
- `docs/templates/Codex完成报告/W-TEST-BUG-018-032-001-完成报告.md`

## 3. 未修改的关键区域

- 未修改 `main.py`：是
- 未修改 `app/**`：是
- 未修改 `web/static/**`：是
- 未修改 `community-site/**`：是
- 未修改 `supabase/**`：是

## 4. 运行的命令

```bash
python -m pytest tests/test_mic_mode.py tests/test_mic_utterance.py -q
```

## 5. 构建/测试结果

| 检查项 | 结果 | 说明 |
|--------|------|------|
| scoped pytest（两文件） | 通过 | **17 passed** |
| boundary_guard | 未跑 | 纯 `tests/` + 文档，未触达 Web/API 边界 |

## 6. 手动验证步骤

| 步骤 | 预期 | 实际 | 通过 |
|------|------|------|------|
| 1 | scoped pytest 两文件 | 全部通过 | **17 passed** | 是 |
| 2 | Windows + 蓝牙，启停弹幕 5 次设备不断连（BUG-032） | 待负责人 | 待负责人 | — |
| 3 | 开麦 ≥30 min 观察 sounddevice status / 卡顿（RISK-017） | 待负责人 | 待负责人 | — |

## 7. 风险与注意事项

- 压测 `none_count < iterations//10` 为防锁竞争退化，非生产 SLA。
- ring buffer 达容量上限时 `filled_bytes` 用 `>=` 断言（回调仍存活代理）。
- 30 min+ Windows audio dropout **未**纳入 CI；RISK-017 仍须负责人 soak。

## 8. 发现但未处理的问题

| 问题 | 说明 | 已记录 |
|------|------|--------|
| `BUGS-OVERVIEW.md` / `P1-HIGH.md` 仍标 BUG-018 待修复、400ms | 以 `main.py` 600/250 ms 为准 | 否 |
| runtime dropout 计数器 + `/api/diagnostics` | 需另票触达 `app/application/` | 否 |
| BUG-032 真机蓝牙断连 | 本票只锁 `_sync_mic_service` 不 `sync(False)` 契约 | 否 |

## 9. scoped diff 结论

本票改动路径：

- `tests/test_mic_utterance.py`
- `tests/test_mic_mode.py`
- `docs/bug-audit/TEST-GAPS.md`
- `docs/当前仓库状态.md`
- `docs/工单列表.md`
- `docs/templates/Codex完成报告/W-TEST-BUG-018-032-001-完成报告.md`

未触及 `app/**`、`main.py`、`web/static/**`、`community-site/**`、`supabase/**`。

## 10. 已更新的文档

- [x] [docs/bug-audit/TEST-GAPS.md](../../bug-audit/TEST-GAPS.md)
- [x] [docs/当前仓库状态.md](../../当前仓库状态.md)
- [x] [docs/工单列表.md](../../工单列表.md)
- [x] 本完成报告

## 11. 建议下一个工单

- TEST-GAPS §6 剩余：BUG-073 pending meta 并发、`BUG-072` invoke_on_main 负载等。
- 若需生产级 dropout 指标：另开 `W-MIC-DIAG-*`（`MicCaptureService` 计数 + diagnostics 快照）。
