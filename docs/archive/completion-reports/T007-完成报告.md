# Codex 完成报告

> 工单 ID：T007-split-low-risk-utils  
> 完成时间：2026-06-03  
> 执行者：Composer / IDE Agent

---

## 1. 修改摘要

按工单将 `app/translations.py`（约 687 行）与 `app/personae.py`（约 630 行）拆分为按域/职责的子模块，入口文件保留 `TRANSLATIONS` 合并与 `Translator`/`tr`，以及 `app.personae` 全量 re-export，**不改动**现有 `from app.translations` / `from app.personae` 调用方。内置人格数据迁至 `data/personae_builtin.json`；版本列表经 `app/persona_version_history` 门面委托 `TemplateManager.versions`。全量 pytest 与 Boundary Guard 通过，无主链路行为变更。

## 2. 修改的文件

- `app/translations.py`（薄入口：合并子模块 + Translator）
- `app/translations_settings.py`（新建）
- `app/translations_danmu.py`（新建）
- `app/translations_ui.py`（新建）
- `app/personae.py`（薄入口：re-export）
- `app/persona_builtin.py`（新建）
- `app/persona_contract.py`（新建）
- `app/persona_manager.py`（新建）
- `app/persona_version_history.py`（新建）
- `data/personae_builtin.json`（新建）
- `app/web_api/persona.py`（`list_versions` 接线门面）

## 3. 未修改的关键区域

- 未修改 `main.py`：是
- 未修改 `app/danmu_engine.py`：是
- 未修改 `app/overlay.py`：是
- 未修改 `app/ai_client.py`：是
- 未修改 `app/templates.py`：是
- 未修改 `web/`：是
- 未顺手重构无关代码：是（`app/` 仅在工单允许范围内改动）

## 4. 运行的命令

```bash
python -m pytest tests/ -q
python -m pytest tests/test_translations.py tests/test_reply_contract.py tests/test_web_persona_api.py tests/test_reply_parser.py -q
python scripts/boundary_guard.py
ruff check app/translations*.py app/persona*.py app/personae.py app/web_api/persona.py --fix
```

## 5. 构建/测试结果

| 检查项 | 结果 | 说明 |
|--------|------|------|
| pytest（全量） | 通过 | 874 passed, 5 skipped |
| pytest（重点） | 通过 | 42 passed（翻译 / 契约 / 人格 Web / PersonaManager） |
| boundary_guard | 通过 | Boundary Guard: PASS |
| ruff（本工单文件） | 通过 | 仅对 T007 改动文件 `--fix`，无剩余问题 |
| ruff（全仓库） | 未作为门禁 | `main.py` 等存在既有 import 告警，非本工单引入 |

拆分后各 Python 模块行数均 &lt; 500（`persona_contract.py` 约 249 行；`translations_*.py` 为按域 dict，入口约 60 行）。

## 6. 手动验证步骤

| 步骤 | 预期 | 实际 | 通过 |
|------|------|------|------|
| 1. 运行应用，切换中/英文 | UI 文案正常 | 未在本环境启动 GUI；自动化翻译/契约/人格测试通过 | 待负责人 |
| 2. 人格列表 / 编辑 / 版本历史 | 功能正常 | `test_web_persona_api` 等通过 | 是（自动化） |
| 3. 确认拆分文件 &lt; 500 行 | 符合验收 | 已核对行数 | 是 |

## 7. 风险与注意事项

- **风险等级**：低（数据搬迁 + import 重组，无 protected call chain 变更）。
- **兼容性**：所有外部 import 仍指向 `app.translations` / `app.personae`。
- **数据**：`personae_builtin.json` 须与旧 `BUILTIN_PERSONAE` 一致；`test_test1_persona_strip_roundtrip` 等已覆盖。
- **可读性**：`translations_*.py` 使用单行 `repr` dict，行数达标但 diff 可读性一般；后续可按 key 分行格式化（非本工单范围）。

## 8. 发现但未处理的问题

无。

| 问题 ID | 简述 | 已记录 |
|---------|------|--------|
| — | — | — |

## 9. 已更新的文档

- [ ] [docs/当前仓库状态.md](../../当前仓库状态.md)
- [ ] [docs/工单列表.md](../../工单列表.md)
- [x] 本完成报告：`docs/templates/Codex完成报告/T007-完成报告.md`

## 10. 建议下一个工单

- 继续 phase-3 模块拆分（若工单序列中有 T008+）。
- 可选：将 `translations_*.py` 格式化为多行 dict，便于人工审阅（纯风格，无行为变更）。
