# 工单 W-019：P0 死代码与场景死状态清理

> **执行者**：Codex / IDE Agent  
> **状态**：已完成  
> **模板来源**：[工单模板.md](工单模板.md)

---

## 工单 ID

**W-019**

## 工单标题

P0 确认无调用死代码 + main 场景死状态清理

## 背景

全库静态审计（2026-05-29）发现：`DanmuHistory` 从未被生产调用（历史上屏走 `HistoryWriter`）；`get_catalog_for_platform`、`list_providers`、`infer_visual_update_from_batch` 等零引用；`main` 中场景 gate 字段仅初始化/重置且 `_scene_api_block_reason()` 恒返回 `""`，与 [runtime-state-map.md](../../runtime-state-map.md) 描述不一致。

## 目标

1. 生产路径仅通过 `app/history_writer.py` 写 `history` 表；`main` 不再实例化 `DanmuHistory`。
2. 删除确认无调用的 `app/` 符号与 `app/history.py`。
3. 清理 `main.py` 无调用方法与场景死状态字段；保留 `_scene_generation` 等诊断字段，**不改变**「截图不递增代际」行为。
4. 同步维护者文档；范围外场景探测恢复记入 ISSUE-014。

## 依赖项

无（W-001/W-002 已完成）

## 允许修改的区域

- `main.py`
- `app/history.py`（删除）
- `app/model_catalog.py`、`app/model_providers.py`、`app/mic_prompt.py`、`app/memory/visual_update.py`、`app/live_freshness.py`
- `scripts/boundary_guard.py`（`history.py` 白名单）
- `tests/`（相关）
- `docs/runtime-state-map.md`、`docs/CAPTURE_AND_DANMAKU_REFERENCE.md`、`docs/audits/token-consumption-audit.md`
- `docs/工单列表.md`、`docs/当前仓库状态.md`、`docs/已知问题与后续事项.md`、`docs/templates/**`

## 禁止修改的区域

- `web/static/`、`app/web_api/`、`app/overlay.py`、`app/danmu_engine.py`、`app/ai_client.py`、`app/runnable.py`
- `requirements.txt`、CI 配置

## 需求

1. 删除 `app/history.py` 与 `main` 对 `DanmuHistory` 的引用。
2. 删除：`get_catalog_for_platform`、`list_providers`、`build_mic_insert_block`、`append_mic_context_to_user_pt`、`infer_visual_update_from_batch`、`SCENE_CHANGE_DEBOUNCE_SEC`、`SCENE_CHANGE_FORCE_DIST`。
3. `main` 删除：`_capture_frame_hash`、`_freshness_mode`、`_should_clear_batch_on_scene_change`、`_rhythm_cooldown_left_ms`；死状态字段及 reset。
4. `_log_api_schedule` 中 `cooldown_left_ms` 固定为 `0`。
5. 更新相关测试与 `boundary_guard` 白名单。

## 非目标

- 不接线 `is_model_slow` / `build_local_fallback_batch`（ISSUE-003 / W-003）
- 不恢复 `_probe_scene_change`（ISSUE-014 / W-020）
- 不删 `_is_reply_stale` 桩（W-001 既定恒不过期）

## 验收标准

- [x] `rg` 无 `DanmuHistory`、`infer_visual_update_from_batch`、`get_catalog_for_platform`、`list_providers`、`_capture_frame_hash`
- [x] `python -m pytest tests/ -q`（674 passed，8 failed 为环境/既有用例，见完成报告）
- [x] `python scripts/boundary_guard.py` PASS
- [x] `test_capture_does_not_advance_scene_generation` 仍通过

## 手动验证步骤

1. `python main.py` 启动，Web + Overlay 正常，启停一轮弹幕。
2. 确认 `history` 表仍有新行（`HistoryWriter`）。
3. `DANMU_API_SCHEDULE_DEBUG=1` 下视觉请求调度 debug 日志正常。

## 风险点

- 误删仍被引用的符号 → 全量 pytest + boundary_guard。
- W-001 曾保留 `_scene_rhythm_pause_until`；当前无写入方，删除为零行为变更。

## 完成后必须更新的文档

- [ ] [docs/当前仓库状态.md](../../当前仓库状态.md)
- [ ] [docs/工单列表.md](../../工单列表.md)
- [ ] [docs/已知问题与后续事项.md](../../已知问题与后续事项.md)

## Codex 完成报告要求

- [templates/Codex完成报告/W-019-完成报告.md](../Codex完成报告/W-019-完成报告.md)
