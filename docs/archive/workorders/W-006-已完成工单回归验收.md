# 工单 W-006：已完成工单自动化回归验收

> **执行者**：Codex / IDE Agent  
> **状态**：已完成  
> **模板来源**：[工单模板.md](工单模板.md)

---

## 工单 ID

**W-006**

## 工单标题

已完成工单（W-001/W-002/W-004 + WebSocket）自动化回归验收与文档校正

## 背景

[`docs/templates/Codex完成报告/`](../Codex完成报告/) 中 W-001 曾未跑全量测试即标完成；WebSocket 修复无正式工单且无回归测试；W-004 测试计数与仓库状态不一致。负责人要求排除隐藏 bug，需一次集中自动化验收并固化 WS 防回归。

## 目标

1. 全量 `pytest` + `boundary_guard` 通过，并在完成报告中记录实际通过数。
2. 新增 `/ws/status` WebSocket 升级回归测试（TestClient），防止 FastAPI `@app.websocket` 403 复发。
3. 校正文档中错误的测试计数与 W-001 测试状态描述。
4. 输出验收结论：哪些「已完成」可标为自动化已验收，哪些仍待负责人手动项。

## 依赖项

无（不依赖 MiMo API Key）。

## 允许修改的区域

- `tests/test_web_console.py`
- `docs/工单列表.md`
- `docs/当前仓库状态.md`
- `docs/templates/Codex完成报告/W-001-完成报告.md`
- `docs/templates/Codex完成报告/W-004-完成报告.md`
- `docs/templates/Codex完成报告/Web控制台WebSocket修复-完成报告.md`
- `docs/templates/工单/W-006-已完成工单回归验收.md`
- `docs/templates/Codex完成报告/W-006-完成报告.md`

## 禁止修改的区域

- `main.py`
- `app/`（业务逻辑）
- `web/static/app.js`
- `requirements.txt`、CI 配置
- ISSUE-003~007 的代码修复

## 需求

1. 运行并记录全量 pytest + boundary_guard + W-001 相关子集。
2. 在 `tests/test_web_console.py` 增加 WS accept 与 1008 拒绝回归测试。
3. 校正 W-001/W-004/WebSocket 完成报告与 `docs/当前仓库状态.md` 测试数字。
4. 登记 WebSocket 调试交付为 W-006 retroactive 验收。

## 非目标

- 不实现 ISSUE-003 本地 fallback 上屏。
- 不补充 MiMo 目录（ISSUE-004）或识图 probe（ISSUE-005）。
- 不使用真实 MiMo API Key 做手动弹幕验收。

## 验收标准

- [x] `python -m pytest tests/ -q` 全绿（641 passed）
- [x] `python scripts/boundary_guard.py` exit 0
- [x] 新增 WS 测试在无真实 DanmuApp 进程下可重复通过
- [x] 完成报告与仓库状态测试数字一致
- [x] ISSUE-003~007 未被误标为已修复

## 手动验证步骤

1. `python -m pytest tests/test_web_console.py -k websocket -v`
2. `python -m pytest tests/ -q`
3. `python scripts/boundary_guard.py`

## 风险点

- TestClient WebSocket 与 uvicorn 运行时略有差异；浏览器 Console 目视仍为可选负责人项。

## 完成后必须更新的文档

- [x] [docs/当前仓库状态.md](../../当前仓库状态.md)
- [x] [docs/工单列表.md](../../工单列表.md)
- [x] W-006 完成报告 + 历史完成报告校正
