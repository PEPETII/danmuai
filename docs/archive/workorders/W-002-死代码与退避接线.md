# 工单 W-002：死代码清理 + 截图退避最小接线

> **执行者**：Codex / IDE Agent  
> **状态**：已完成  
> **模板来源**：[工单模板.md](工单模板.md)

---

## 工单 ID

**W-002**

## 工单标题

清除库存预取死代码、删除 ProbeRunnable、接线截图退避间隔

## 背景

- [token-consumption-audit.md](../../audits/token-consumption-audit.md) 确认：`_should_request_new_batch`、`_schedule_next_screenshot` 无生产调用方；`ProbeRunnable` 未引用；`_screenshot_backoff_level` 变化但 `_apply_screenshot_interval_backoff` 未使用 `screenshot_interval_ms`。
- W-001 已清除 realtime/节奏模式主链路；本工单延续同类误导性遗留。

## 目标

1. 删除 main.py 库存预取死链路与 `_screenshot_scheduled` 等仅服务死链的状态。
2. 删除 `app/probe_runnable.py`。
3. `_apply_screenshot_interval_backoff` 接入 `live_freshness.screenshot_interval_ms` 并输出结构化日志。
4. 保留 `is_model_slow` / `build_local_fallback_batch`（仅单测），文档标明未接入 main。

## 允许修改的区域

- `main.py`
- `app/probe_runnable.py`（删除）
- `app/live_freshness.py`（注释/docstring）
- `tests/` 相关
- `docs/runtime-state-map.md`、`docs/main-pipeline-sequence.md`、`docs/当前仓库状态.md`、`docs/工单列表.md`、`docs/已知问题与后续事项.md`

## 禁止修改的区域

- `web/static/`、`app/web_api/`、`app/overlay.py`、`app/danmu_engine.py`、`app/ai_client.py`、`app/runnable.py`
- `requirements.txt`、CI 配置

## 非目标

- 不实现本地 fallback 上屏（`build_local_fallback_batch` 不接 main）
- 不改截图 → AI → 入队 → 上屏主链路顺序
- 不改 QTimer/QThreadPool/线程模型

## 验收标准

- [x] 生产代码无 `_should_request_new_batch`、`_schedule_next_screenshot`、`ProbeRunnable` 等
- [x] 退避后 `screenshot_timer.interval()` 按 `screenshot_interval_ms` 缩放
- [x] `pytest` 与 `boundary_guard` 通过
